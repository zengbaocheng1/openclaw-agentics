```markdown
# Heartbeat Tasks

## OpenRouter 免费模型监控
- 运行 `bash ~/.openclaw/workspace/config/openrouter-monitor.sh`，如果输出有变化（新增/下架），把完整输出通知主人
- 每 3 小时检查一次即可（上次检查时间记录在 config/openrouter_last_check.txt）
```

## Related

- [Heartbeat config](/gateway/config-agents)
